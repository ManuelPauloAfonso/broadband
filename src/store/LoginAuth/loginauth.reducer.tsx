

export default function Addlogin(state = [], action) {
    return {
        switch (action.type) {
            case 'LoginAuth':
                return false

                default: 
                return state

        }
    }
}